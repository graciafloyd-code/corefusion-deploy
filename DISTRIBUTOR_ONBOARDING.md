# CoreFusion 分销商接入操作手册

## 1. 接入目标

每个分销商使用一个独立 Token 接入主实例。主实例负责控制：

- 分销商可用额度
- 分销商可用模型
- 分销商计费倍率
- 请求日志与消耗统计
- 上游真实 API 调用

请求链路：

```text
终端用户 -> 分销商 OEM 实例 -> CoreFusion 主实例 -> 真实 API
```

主实例 API 地址：

```text
https://supchuang.com/v1
```

## 2. 主实例侧配置

路径：后台 -> 用户管理

为每个分销商单独创建一个用户，建议命名格式：

```text
dealer_公司名
dealer_渠道名
```

创建后需要设置：

| 配置项 | 用途 |
| --- | --- |
| 用户余额 / 额度 | 控制分销商最多可消耗多少 |
| 用户分组 | 控制批发计费倍率 |
| 可用模型 | 控制分销商可转售哪些模型 |
| 用户状态 | 可随时启用或禁用分销商 |

建议不要让多个分销商共用同一个用户或 Token，否则后续无法准确统计用量，也不方便单独停用。

## 3. 分组与批发价

当前主实例建议使用三档分组：

| 分组 | 适用对象 | 倍率 |
| --- | --- | --- |
| standard | 普通代理 | 1.4 |
| pro | 专业代理 | 1.25 |
| strategic | 核心代理 | 1.1 |

分销商用户分到哪个组，就按该组倍率计费。

示例：

```text
dealer_a -> standard
dealer_b -> pro
dealer_c -> strategic
```

## 4. 创建分销商专属 Token

路径：用户控制台 -> 令牌

每个分销商创建一个独立 Token：

1. 登录该分销商对应用户。
2. 进入令牌页面。
3. 点击创建令牌。
4. 设置令牌名称，例如：

```text
dealer_a_oem
```

5. 设置额度限制。
6. 保存后复制 Token。

Token 只交给对应分销商，不要复用给其他渠道。

## 5. 模型范围控制

路径：后台 -> 渠道管理 / 模型配置 / 分组配置

给分销商开放模型时，需要同时确认：

- 主实例渠道支持该模型。
- 该模型已配置价格或倍率。
- 分销商所在分组允许访问该模型。
- 分销商 Token 没有额外限制该模型。

建议先开放少量稳定模型，例如：

```text
deepseek-v4-pro
```

确认调用和扣费正常后，再逐步增加其他模型。

## 6. 分销商 OEM 实例配置

在分销商的渠道配置中填写：

| 配置项 | 填写值 |
| --- | --- |
| 渠道类型 | OpenAI 兼容 |
| API Base URL | https://supchuang.com/v1 |
| API Key | 主实例分配给该分销商的专属 Token |
| 模型列表 | 主实例允许该分销商使用的模型 |

示例：

```text
渠道类型：OpenAI 兼容
API Base URL：https://supchuang.com/v1
API Key：sk-xxxxxxxxxxxxxxxx
模型列表：deepseek-v4-pro
```

分销商保存渠道后，应在其 OEM 后台进行一次模型测试。

## 6.1 分销商只有域名时的处理方式

如果分销商只有一个域名或网址，还没有后台系统，则不能只靠 Token 直接生产。需要先为该分销商部署一套独立 OEM 实例。

推荐架构：

```text
终端用户 -> 分销商域名 -> 分销商 OEM 实例 -> https://supchuang.com/v1 -> 真实 API
```

分销商 OEM 实例可以部署在：

- 分销商自己的 VPS
- 你统一托管的 VPS
- 你为分销商单独购买的 VPS

如果由你统一托管，建议每个分销商至少独立以下内容：

| 项目 | 建议 |
| --- | --- |
| 域名 | 使用分销商自己的域名 |
| 容器名 | 独立命名，例如 dealer-a-new-api |
| 数据库 | 独立数据库或独立 MySQL 实例 |
| Redis | 可独立，也可按规模共用 |
| 管理员账号 | 独立账号 |
| 上游 Token | 独立 Token |
| 品牌配置 | 使用分销商自己的名称、Logo、页脚 |

### 开通前需要分销商提供

```text
分销商域名：
品牌名称：
Logo 文件：
管理员账号：
管理员初始密码：
是否允许用户注册：
默认模型列表：
预计首充额度：
```

如果域名托管在分销商自己的 DNS 面板，需要让他添加：

```text
A      @      你的 OEM 服务器 IP
CNAME  www    分销商根域名
```

如果只使用二级域名，例如 `api.example.com`：

```text
A      api    你的 OEM 服务器 IP
```

### 你这边需要完成

1. 给分销商部署独立 OEM 实例。
2. 给该实例配置域名和 HTTPS。
3. 在你的主实例创建分销商用户。
4. 给分销商用户设置额度、分组和可用模型。
5. 创建该分销商专属 Token。
6. 在分销商 OEM 实例中添加 OpenAI 兼容渠道：

```text
API Base URL：https://supchuang.com/v1
API Key：该分销商专属 Token
模型列表：允许转售的模型
```

7. 在分销商 OEM 实例中测试模型调用。
8. 回到主实例确认日志和扣费归属正确。

## 7. 测试流程

主实例创建好分销商用户和 Token 后，按以下流程测试：

1. 在主实例确认分销商用户余额充足。
2. 在主实例确认该用户分组正确。
3. 在分销商 OEM 实例配置渠道。
4. 在分销商 OEM 实例发起模型测试。
5. 回到主实例查看日志。
6. 确认该请求消耗记录归属到对应分销商用户。
7. 确认扣费倍率符合该分销商分组。

测试 curl：

```bash
curl https://supchuang.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 分销商专属Token" \
  -d '{
    "model": "deepseek-v4-pro",
    "messages": [
      {
        "role": "user",
        "content": "测试连接"
      }
    ]
  }'
```

## 8. 日常运营

### 查看分销商消耗

路径：后台 -> 日志 / 用户管理

重点查看：

- 请求次数
- 消耗额度
- 使用模型
- 错误请求
- 单日消耗趋势

### 调整分销商额度

路径：后台 -> 用户管理

可根据合同或预付款情况调整余额。余额不足时，分销商下游用户会调用失败。

### 调整分销商可用模型

路径：后台 -> 渠道管理 / 模型配置 / 用户或分组权限

新增模型后建议先给内部测试账号验证，再开放给分销商。

### 停用分销商

如需暂停某个分销商：

1. 禁用该分销商用户，或
2. 删除该分销商 Token，或
3. 将该用户余额调为 0。

优先建议禁用 Token 或用户，便于保留历史记录。

## 9. 风险控制建议

- 每个分销商必须使用独立用户和独立 Token。
- 给 Token 设置额度上限，避免异常消耗。
- 先小额度测试，再正式放量。
- 不要把管理员 Token 给分销商。
- 不要让分销商直接接触真实上游 API Key。
- 分销商出现异常请求时，可立即禁用其 Token。
- 定期导出或备份消费记录，方便对账。

## 10. 分销商交付信息模板

交付给分销商时，可按以下格式发送：

```text
渠道类型：OpenAI 兼容
API Base URL：https://supchuang.com/v1
API Key：请使用单独发送的专属 Token
可用模型：deepseek-v4-pro
额度上限：以双方约定为准
计费倍率：以双方约定为准
```

注意：API Key 建议通过安全渠道单独发送，不要放在公开文档或群公告中。
